
import {AppBar,Toolbar} from '@mui/material';
const Header  = ()=> {
    return(
        <AppBar>
            <Toolbar>

            </Toolbar>
        </AppBar>
        
    )
}

export default Header;